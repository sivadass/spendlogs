import React from "react";
import { FixedContainer, PageTitle, Wrapper } from "../styled/common";

const Register = () => {
  return (
    <FixedContainer>
      <PageTitle>Register</PageTitle>
      <Wrapper>Register now to access your expense book</Wrapper>
    </FixedContainer>
  );
};

export default Register;
